# Earlier prototypes

This directory preserves EcoLearn's earlier web and scanner prototypes. They are excluded from production deployments and current verification workflows. The maintained applications are `apps/platform-web` and `apps/mobile-ecolearn`; their backend is defined in `apps/platform-web/supabase`.
