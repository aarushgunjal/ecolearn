{
  "name": "ecolearn",
  "private": true,
  "version": "1.1.0",
  "description": "EcoLearn web, native applications, and shared backend",
  "engines": { "node": ">=22.12.0" },
  "scripts": {
    "setup:web": "npm ci --prefix apps/platform-web",
    "setup:mobile": "npm ci --prefix apps/mobile-ecolearn",
    "dev": "npm run dev --prefix apps/platform-web",
    "build": "npm run build --prefix apps/platform-web",
    "lint": "npm run lint --prefix apps/platform-web",
    "typecheck": "npm exec --prefix apps/platform-web -- tsc --noEmit -p apps/platform-web/tsconfig.app.json && npm exec --prefix apps/mobile-ecolearn -- tsc --noEmit -p apps/mobile-ecolearn/tsconfig.json",
    "test:database": "npm run test:database --prefix apps/platform-web",
    "test:web": "npm run test:e2e --prefix apps/platform-web",
    "mobile": "npm start --prefix apps/mobile-ecolearn"
  }
}
